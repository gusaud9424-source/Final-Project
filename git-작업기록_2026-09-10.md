# Git 작업 기록 (2026-09-10)

## 1. 커밋
- 대상: `csrf_test.html`(CSRF PoC 테스트 페이지), `피드백.txt`(프로젝트 피드백 메모)
- 커밋 해시: `f5ce4f4`

## 2. 푸시 이슈 및 해결
- **인증 실패**: 원격이 HTTPS인데 저장된 자격증명이 없어 `could not read Username` 에러 발생
  - GitHub Personal Access Token(PAT) 발급 후, `git credential approve`로 자격증명 저장 (토큰은 대화창이 아닌 별도 터미널에서 직접 등록 — 유출 방지)
  - ⚠️ 중간에 사용자가 실수로 토큰을 대화창 명령에 평문으로 입력해 노출된 적 있음 → 즉시 폐기(revoke) 후 재발급 안내함
- **히스토리 불일치(non-fast-forward)**: `git push` 시 원격과 로컬의 커밋 히스토리가 완전히 다른(공통 조상 없는) 별개 히스토리로 확인됨
  - 로컬: Flask 백엔드 + Vue 프론트엔드 기반 실제 프로젝트 구조
  - 원격(origin/main): DVWA/WebGoat/bWAPP 관련 docker-compose, setup.sh (별개 초기 커밋 2개: `da5d584`, `8a3c7ec`)
  - 처리 방안 선택: 원격 `main`은 그대로 두고, 로컬 작업 내용을 **`feature/final-prj` 브랜치로 별도 푸시**

## 3. 결과
- `feature/final-prj` 브랜치에 로컬 작업 내용 푸시 완료
- 푸시 중 GitHub 안내로 저장소 이름 변경 확인: `Final-Prj` → `Final-Project`
- 원격 URL 갱신: `origin` → `https://github.com/gusaud9424-source/Final-Project.git`

## 4. 남은 작업 (참고)
- 원격 `main`(DVWA/WebGoat/bWAPP)과 `feature/final-prj`(현재 프로젝트) 병합 여부는 GitHub에서 PR로 검토 후 결정 필요
